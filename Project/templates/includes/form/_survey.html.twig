{% if form != null and question != null and response != null %}
	<h2>Votre avis compte</h2>

	{{form_start(form)}}
	<legend>{{ question.question }}</legend>
	{% for item in response %}
		<div class="response">
			<input type="radio" id="radio_{{item.id}}" name="radio_response" value="{{ item.text }}">
			<label for="radio_{{item.id}}">{{ item.text }}</label>
		</div>
	{% endfor %}
	<button type="submit">Envoyer ma réponse</button>
	{{form_end(form)}}
{% endif %}
